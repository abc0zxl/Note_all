## 请求方式

1.**Post**：它不会讲请求参数换成，而且是放到body来传递的，安全系数更高。

2.**Get**：请求参数就放在请求

3.**Patch**：这是用于更新数据的请求方式，要求只修改传过去的参数

4.**Delete**

5.**Put**：也是用于更新数据的请求方式，要求更新所有字段

6.****

HTTP结构

1.**header**：请求头

* 在url下方，body上方，一个单独的区域，用于描述这个请求的属性，
* 存放Host
* Authorization
* Content_Type
* Accept

2.**body**：请求体，不可见

* 用于承载业务数据

3.param：

3.URL：这个就是那个连接，可以用于存放param

* 协议
* 域名
* 路径
* 查询参数
