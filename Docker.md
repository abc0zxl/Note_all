# Docker

**问题**：

* **环境问题**：不同电脑或者服务器上，环境配置不同，导致程序无法运行
* **依赖冲突**：缺少依赖，版本冲突，运行出错

**解决**：

* **打包方式**：程序和依赖环境一起打包，确保在任何环境都可以运行
* **镜像**：无需手动配置环境，提高开发效率

**工作原理**：

* 把程序和其运行环境依赖打包成镜像（盒子），实现环境一致性。


### 容器

他是镜像运行的实例

### Docker

负责容器的创建和管理

### Docker仓库

用于存储和分法镜像

* Docker Hub
* 阿里云镜像仓库
