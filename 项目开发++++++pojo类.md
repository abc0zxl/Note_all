# pojo类

1.**entity**：

**作用**：用于存储数据库表的，会将整个表的属性记录过来，一般只记录会用到的表

2.**DTO**：

**作用**：数据交互类，当与前端交互的属性和实体中的数量差距比较大的时候，就会按需封装这部分属性作为一个DTO文件。

* **query**：查询DTO，用于查询操作，数据库会饭回来数据
* **request**：请求DTO，

3.**VO**：
